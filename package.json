{
  "name": "ai-privacy-security-internal-audit-system",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix backend run prisma:generate && npm --prefix frontend run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
